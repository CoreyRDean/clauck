# Editor / OS cruft
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Claude Code agent scratch space (isolated worktrees from `Agent({isolation:"worktree"})`)
.claude/worktrees/

# Never commit a user's runtime state
*.log
.state/
.manifest.json
